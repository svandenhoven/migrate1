################################################################################
# deploy stage jobs
################################################################################

pages:
  extends:
    - .rules_prod
  stage: deploy
  script:
    - echo "Pages accessible through ${CI_PAGES_URL}"
  artifacts:
    paths:
      - public
    expire_in: 1d
